# SynAP-Fib 维护规则

- 统一维护论文与代码；所有文本按 UTF-8 读写，默认无 BOM。
- 当前论文入口是 `paper/zh/main.tex`，英文主稿和共享图片、参考文献在 `paper/en/`。现有英文权威性说明不因目录迁移而改变。
- `code/` 为开发代码，遵循其 AGENTS.md 与 EVALUATION_POLICY.md；导入时活跃配置为 AE_COR_v2_seed2026_lambda。
- `reproducibility/ae_cor_v2_seed2026/` 是冻结来源档案，保持逐字节不变。其 AGENTS.md 仅描述该历史快照，不决定当前开发轮次。
- 论文中的结果不得静默替换为后续实验轮次。保留 train -> val 选择 -> 冻结 -> test 的规则与指标方向。
- 不上传数据集、患者清单、逐图预测、checkpoint、密钥、缓存和训练日志。
- 论文方法、数值、图表和声明发生修改时检查中英文一致性；图片与参考文献共用英文目录。
- 新仓库是后续维护入口，原工作区目录保留作来源；不自动双向同步，也不修改原冻结实验。
