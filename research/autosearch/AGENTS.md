# SynAP autosearch v1

Follow PROTOCOL.md and REQUEST.md in this directory. This is an independent
research round; code/ remains a read-only dependency with its own old-round rules.
Never import the legacy runner/queue or read historical test results in search.
Keep all data, trial states, predictions, weights and logs under outputs/ (ignored).
Use UTF-8 without BOM. Never modify historical snapshots, paper results or data.
